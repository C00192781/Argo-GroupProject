#pragma once
#include "Instance.h"

class GameOverInstance : Instance
{
public:
	GameOverInstance();
	~GameOverInstance();
	void Update() {};
	void Update(float deltaTime);
	void Generate();
private:
	

};
